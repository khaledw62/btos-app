import 'package:flutter/cupertino.dart';

class TestModel {
  String image, title, distance;
  Color color;

  TestModel({
    required this.image,
    required this.title,
    required this.distance,
    required this.color,
  });
}
